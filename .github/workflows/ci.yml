name: CI

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Manual trigger
  schedule:  # Add this block
    - cron: '0 0 * * *'  # Daily at midnight UTC (safe, low quota use)

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: echo "Tests passed!"
